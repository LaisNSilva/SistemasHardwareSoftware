int level10(){
    int si;
    scanf("%d", &si);

    int dx = 0;

    int ax = dx;

    int cx = vec[];
    ax = cx[dx]; //LAIS

    while(ax !=0){
        si-=ax;
        dx+=1;
        ax = vec[dx];

    }

    ax = (int)3==si;
    return ax;

    


}